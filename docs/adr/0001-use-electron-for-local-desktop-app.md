# Use Electron for the Local Desktop App

OrbitSSH will use Electron as the local desktop shell for the React application because the product needs direct, reliable access to local files, saved credentials, SSH keys, persistent connection processes, and desktop debugging tools. Tauri would likely produce a smaller app, but Electron gives this project a more direct path through the Node ecosystem for SSH orchestration and local AI tool integration.
